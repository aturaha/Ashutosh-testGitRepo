package com.example.demo.model;

//package com.gymnasticbodies.my.foundcoreservice.entity;

import com.fasterxml.jackson.annotation.JsonManagedReference;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import javax.persistence.*;
import java.util.*;

@NoArgsConstructor
@Getter
@Setter
@Entity
@Table(name="exercises")
public class Exercises {
     @Id
     private Integer exerciseId;
     private String name;
     private String imName;
     private String element;
     private String elementFullName;
     private Integer progressionLevel;
     private String imMastery;
     private String notation;
	/*
	 * @ManyToOne
	 * 
	 * @JoinColumn(name = "courseId") private Courses courseInfo;
	 * 
	 * @JsonManagedReference
	 * 
	 * @OneToMany(cascade = CascadeType.ALL, fetch = FetchType.LAZY, mappedBy =
	 * "exerciseId") private List<ExercisesFocusPoints> exercisesFocusPoints = new
	 * LinkedList<>();
	 * 
	 * @JsonManagedReference
	 * 
	 * @OneToMany(cascade = CascadeType.ALL, fetch = FetchType.LAZY, mappedBy =
	 * "exerciseId") private List<ExercisesInstructions> exercisesInstructions = new
	 * LinkedList<>();
	 * 
	 * @JsonManagedReference
	 * 
	 * @OneToMany(cascade = CascadeType.ALL, fetch = FetchType.LAZY, mappedBy =
	 * "exerciseId") private List<ExercisesVideos> exercisesVideos = new
	 * LinkedList<>();
	 * 
	 * @ManyToOne
	 * 
	 * @JoinColumn(name = "masterySetId") private MasterySets masterySets;
	 * 
	 * 
	 */}


/*
 * public class Exercises {
 * 
 * }
 */
